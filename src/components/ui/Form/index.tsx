import { FormItem } from "./FormItem";
import { FormRoot } from "./FormRoot";
import { FormSection } from "./FormSection";

export const Form = {
  Root: FormRoot,
  Item: FormItem,
  Section: FormSection,
};
